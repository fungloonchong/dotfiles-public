# Allow color for curses tui in tmux
set -g default-terminal 'screen-256color'
set -g terminal-overrides ',screen-256color:Tc'
# Start windows and panes at 1, not 0
set -g base-index 1
setw -g pane-base-index 
